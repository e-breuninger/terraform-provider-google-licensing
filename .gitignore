# Compiled provider binary
terraform-provider-google-licensing
terraform-provider-google-licensing_*

# Terraform working directories
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.*
**/terraform.tfstate.*.backup
**/crash.log
**/crash.*.log
**/.claude/

# GoReleaser dist output
dist/

# IDE
.idea/
.vscode/
*.swp

# Secrets — never commit private keys
*.asc
gpg-private-key*
find.txt

# Local test config — contains real project/user data, never commit
test/main.tf
test/licenses
